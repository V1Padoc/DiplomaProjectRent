const path = require('path');

module.exports = {
  // Говоримо CLI, де знаходиться файл, який експортує налаштований екземпляр sequelize.
  // Це найважливіший рядок!
  'config': path.resolve('config', 'database.js'),

  // Вказуємо шляхи до інших папок (напевно, вони у вас і так стандартні, але це гарна практика)
  'models-path': path.resolve('models'),
   // якщо є
  'migrations-path': path.resolve('migrations')
};